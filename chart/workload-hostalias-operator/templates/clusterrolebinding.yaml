apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "workload-hostalias-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "workload-hostalias-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "workload-hostalias-operator.fullname" . }}-manager-role
subjects:
  - kind: ServiceAccount
    name: {{ include "workload-hostalias-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
